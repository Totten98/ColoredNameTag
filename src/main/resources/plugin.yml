name: ${project.artifactId}
main: it.totten98.colorednametag.ColoredNameTag
version: ${project.version}
authors: [Totten98, SimoSbara (Giuly), Axel]
website: https://www.totten98.it/
depend: [Vault]
description: Create a team and assign a color for each permission defined on config.
load: STARTUP
database: false
prefix: ${project.artifactId}
api-version: 1.16